spring:
  datasource:
    url: jdbc:mysql://localhost:2104/realestatedatabase
    username: realEstateUser
    password: tara_tinjon2104
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    serialization:
      write-dates-as-timestamps: false

#upload:
#  directory: "D:\Real Estate Management System\Uploaded Images"

profile: { pictures: { directory: "D:/Real Estate Management System/Uploaded Images/Profile pictures/" } }
